[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[nixpacks]
# ffmpeg para convertir audio de Telegram (ogg → wav)
aptPkgs = ["ffmpeg"]
